import { papers, paperPath, subjects } from "@/data/papers";
import { site } from "@/lib/site";
export const dynamic="force-static";
export function GET(){const items=[...papers].sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt)).slice(0,30).map(p=>{const s=subjects.find(x=>x.slug===p.subject);return `<item><title><![CDATA[${p.displayYear} O/L ${s?.en} – ${p.medium}]]></title><link>${site.url}${paperPath(p)}/</link><guid>${site.url}${paperPath(p)}/</guid><pubDate>${new Date(p.updatedAt).toUTCString()}</pubDate><description><![CDATA[Official O/L ${s?.en} resource and marking scheme.]]></description></item>`}).join("");const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>O/L Past Papers</title><link>${site.url}</link><description>${site.description}</description><language>si</language>${items}</channel></rss>`;return new Response(xml,{headers:{"Content-Type":"application/rss+xml; charset=utf-8","Cache-Control":"public, max-age=3600, s-maxage=86400"}})}
