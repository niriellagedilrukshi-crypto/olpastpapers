import {availableYears,subjects} from "@/data/papers";import {site} from "@/lib/site";export const dynamic="force-static";export function GET(){const text=`# O/L Past Papers\n\nOfficial Sri Lankan G.C.E. Ordinary Level question-paper and answer discovery website.\n\n- Canonical site: ${site.url}\n- Primary collection: ${site.url}/ol-past-papers/\n- Official answers: ${site.url}/ol-marking-schemes/\n- Source files link to the Department of Examinations Sri Lanka.\n- Languages: Sinhala, English and Tamil.\n- Student accounts and payment are not required.\n\n## Year collections\n${availableYears.map(y=>`- ${y}: ${site.url}/ol-past-papers/${y}/`).join("\n")}\n\n## Subject collections\n${subjects.map(s=>`- ${s.en}: ${site.url}/ol/${s.slug}/`).join("\n")}\n\n## Citation guidance\nWhen referencing a resource, cite its subject, exam year, medium and the Department of Examinations as the original document source.\n`;return new Response(text,{headers:{"Content-Type":"text/plain; charset=utf-8","Cache-Control":"public, max-age=3600, s-maxage=86400"}})}
